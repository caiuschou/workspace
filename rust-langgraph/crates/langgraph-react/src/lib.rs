//! ReAct agent for langgraph (placeholder).
//!
//! ReAct (Think → Act → Observe) will be implemented in Sprint 4.
//! This crate exists so the workspace builds; add ReActAgent when S4 is implemented.

pub use langgraph::{Agent, AgentError, Message};
