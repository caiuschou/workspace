# GitLab MCP demo (react_mcp_gitlab example)
# Copy to workspace root as .env and fill values. Do NOT commit .env or real tokens.

GITLAB_TOKEN=
GITLAB_URL=https://gitlab.com
MCP_SERVER_COMMAND=/path/to/gitlab-mcp-server
